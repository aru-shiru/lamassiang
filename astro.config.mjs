// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Used to build absolute URLs for link-preview tags (og:url, og:image).
  // When you attach a custom domain, change this one line and redeploy.
  site: 'https://lamassiang.dzularsyil-muhammad-32e.workers.dev',
});
